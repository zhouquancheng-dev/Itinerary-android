<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="ActivityDialogStyle">
        <!-- 隐藏 action bar 和 title bar -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- dialog 圆角 -->
        <item name="android:windowBackground">@drawable/jiguang_dialog_shape</item>
        <!-- 点击 dialog 区域外取消 dialog -->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!-- 背景模糊 -->
        <item name="android:windowBlurBehindEnabled" tools:ignore="NewApi">true</item>
        <item name="android:windowBlurBehindRadius" tools:ignore="NewApi">8dp</item>
    </style>
</resources>